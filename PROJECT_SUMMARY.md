# Vocal Studio – 实时练唱音高监测工具

## 项目概述
Vocal Studio 是一个纯前端的实时练唱音高监测工具。它通过浏览器麦克风采集声音，在本地使用 Web Audio API 与 YIN 音高检测算法分析当前音高，并以调音器、钢琴窗轨迹和音域统计的形式反馈演唱状态。

项目不依赖后端服务，不上传录音数据，适合作为声乐练习、音准训练、发声稳定性观察和前端音频处理学习示例。

## 主要功能
- **实时音高检测**：打开麦克风后实时识别当前频率与最接近的十二平均律音名。
- **调音器式音准反馈**：显示当前音高相对标准音的 cents 偏差，并用指针展示偏高或偏低。
- **稳定性评估**：根据最近一段音高偏差的波动程度判断“采样中 / 很稳定 / 较稳定 / 波动明显”。
- **音量监测**：显示实时音量数值，并提供音量条辅助判断输入是否过小。
- **钢琴窗轨迹**：像 Auto-Tune / Melodyne 的音高编辑窗口一样，在时间轴上绘制最近 14 秒的音高曲线。
- **音域切换**：支持男低、男高、女低、女高、宽音域等显示范围。
- **参考音播放**：点击钢琴窗左侧琴键，可播放对应音高的正弦参考音。
- **音域统计**：统计当前监听会话中的最高音、最低音、平均音高和音域跨度。
- **首次使用引导**：第一次打开会显示简短操作说明。
- **日夜间主题**：支持深色与浅色主题切换，并通过 localStorage 记忆选择。
- **PWA 支持**：包含 manifest 与 service worker，可缓存核心静态资源。
- **无障碍优化**：包含 `aria-label`、`aria-live`、跳转主内容链接等基础可访问性设计。
- **响应式布局**：桌面端左侧固定调音器，移动端底部悬浮调音器。

## 技术栈
- **HTML5**：页面结构、语义化区域、PWA manifest 引入。
- **CSS3**：响应式布局、主题变量、动效、视觉系统（含毛玻璃背景、双层贝塞尔卡片等）。
- **JavaScript**：音频采集、YIN 音高检测、Canvas 绘制、状态管理。
- **Web Audio API**：麦克风输入、AnalyserNode 采样、参考音播放。
- **Canvas 2D API**：钢琴窗背景网格与音高曲线绘制。
- **Service Worker / Cache API**：静态资源缓存。
- **localStorage**：保存主题和首次引导状态。

## 项目结构
```
vocal-pitch-monitor/
├── README.md          # 项目说明文档
├── PROJECT_SUMMARY.md # 本项目总结（概览版）
├── index.html         # 页面结构与主要 UI 区块
├── style.css          # 视觉样式、主题、响应式布局
├── app.js             # 音频检测、音高计算、Canvas 绘制和交互逻辑
├── manifest.json      # PWA 应用清单
└── service-worker.js  # 静态资源缓存逻辑
```

## 快速开始
1. **克隆或进入项目目录**
   ```bash
   git clone https://github.com/nggezi/vocal-pitch-monitor.git
   cd vocal-pitch-monitor
   ```
2. **启动本地 HTTP 服务**（出于麦克风权限的安全考虑，建议使用 `localhost` 或 HTTPS 访问）
   ```bash
   python -m http.server 3001
   ```
3. **打开页面**
   访问 http://localhost:3001
4. **开始使用**
   - 点击页面中的“开始监听”。
   - 允许浏览器访问麦克风。
   - 对着麦克风唱一个稳定单音或一句旋律。
   - 观察左侧调音器、钢琴窗轨迹和音域统计。
   - 点击“停止”结束当前监听会话。

## 最近更新（v2.2）
- 调整了音高检测参数（SMOOTH、CENTS_SMOOTH、VOLUME_THR、JUMP_CENTS、HOLD_MS、MEDIAN_WINDOW）以提升灵敏度并保留自然抖动。
- 在 UI 上添加了毛玻璃背景（backdrop‑filter）和半透明深色面板，提升视觉层次感。
- 移除了脉冲动画，保持界面专注且静音。
- 更新 Service Worker 缓存版本至 `vocal-studio-v3`。
- 版本号更新至 v2.2。

## 最近更新（v3.1）
- 修复转音卡顿：将「超过 150 cents 直接丢弃帧」改为「快攻收敛 + 八度跳变两帧确认」，连续旋律不再冻结在旧音高。
- 加快响应：去掉多余的 cents 二次平滑，转音时自动切换到快攻系数，检测延迟明显下降。
- 优化 YIN 性能：限定人声频率搜索范围（55–1500 Hz）、复用缓冲区，降低 CPU 占用。
- 增加周期置信度门控，减少周期性噪声导致的音高乱跳。
- 钢琴窗重绘降至 30fps，缓存主题颜色、去重重复 DOM 写入。
- 更新 Service Worker 缓存版本至 `vocal-studio-v5`。
- 版本号更新至 v3.1。

## 开发说明
- 修改 UI：编辑 `index.html`
- 修改样式：编辑 `style.css`
- 修改音频算法或交互逻辑：编辑 `app.js`
- 项目无构建步骤，无 npm 依赖，浏览器可直接加载源码。

## 许可证
当前仓库未声明开源许可证。如需公开发布或允许他人复用，请补充明确的 `LICENSE` 文件。

## 作者
- 作者：nggezi
- GitHub：https://github.com/nggezi
